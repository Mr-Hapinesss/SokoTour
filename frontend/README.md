This is a tempelate.

How to use
1. BACKEND
    a. npm init -y.
    b. npm install.
    c. install => express, dotenv, mongoose, cookie-parser, bcrypt, cors, and jsonwebtoken. 
    d. Clone the folder and run.


2. FRONTEND
    a. install vite and configure it.
    b. npm install tailwindcss... /vite.
    c. install react-router-dom.
    d. create jsconfig.json, update viteconfig.json and configure them.
    e. apply shadcn components.
    f. clone the repo and run it...

make changes on the backend penye uliweka false.